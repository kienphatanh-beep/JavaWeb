# ===============================
# SERVER CONFIG
# ===============================
server.port=8080

# ===============================
# DATABASE CONFIG (PostgreSQL)
# ===============================
# Sử dụng biến môi trường để linh hoạt giữa Local và Deploy
spring.datasource.url=jdbc:postgresql://${DB_HOST:dpg-d5thmicr85hc73f8rcfg-a.singapore-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:minhkien2}?sslmode=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:9jvGmOSaB2hKNO3pqWCAuXuQx3Ygy9uA}

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
# Đã cập nhật sang Dialect của PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# LOGGING
# ===============================
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===============================
# MULTIPART (FILE UPLOAD LIMIT)
# ===============================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ===============================
# IMAGE PATH (QUAN TRỌNG)
# ===============================
# Lưu ý: Trên Render, thư mục "images/" sẽ bị xóa khi Restart Server
# Nếu muốn lưu lâu dài bạn nên dùng Cloudinary hoặc AWS S3 sau này.
project.image=images/

# ===============================
# JWT CONFIG
# ===============================
jwt_secret=day_la_khoa_bi_mat_rat_dai_va_bao_mat_cho_du_an_didong_2025
jwt_expiration_ms=86400000

# ===============================
# SWAGGER / OPENAPI
# ===============================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.doc-expansion=false